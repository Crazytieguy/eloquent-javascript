(assign total 0)
(assign count 1)
(while (< count 11)
       (assign total (+ total count))
       (assign count (+ count 1)))
(print total)

(print (sum (map (fn (elem)
                     (+ elem 1))
                 (array 1 2 3))))
